spring:
  application:
    name: delivery
  rabbitmq:
    host: ${rabbitmq.host:rabbitmq}
    port: ${rabbitmq.port:5672}
server:
  port: ${port:8084}

logging:
  level:
    com.epages: INFO
    feign: DEBUG
    org.springframework: INFO
